---
description: Wrap up the current working session cleanly so a following /clear loses nothing important
---

Invoke the `end-session` skill to wrap up this session — sync docs and memory, reconcile TODOs, run local quality gates, append to `SESSION-SUMMARIES.md`, and (when the session clearly indicates feature work is complete) open a PR with auto-merge + squash.

Arguments: $ARGUMENTS
