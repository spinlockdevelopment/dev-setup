---
description: Pre-implementation hardening pass on a superpowers plan — cross-model review plus checkpoint-block injection
---

Invoke the `review-plan` skill to review and harden the current implementation plan before execution. Pass along any arguments after the command as the plan path or flags (e.g., `--no-checkpoints`, `--checkpoints`, or an explicit plan file path).

Arguments: $ARGUMENTS
