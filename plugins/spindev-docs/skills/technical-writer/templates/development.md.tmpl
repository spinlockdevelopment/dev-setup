# Development

<!--
  AGENT: derive from README.md / CONTRIBUTING.md / the project manifests.
  Cover:
    - Local setup (toolchain versions, package install, env vars).
    - How to run the code locally (CLI, dev server, tests).
    - The branching / PR workflow (link CONTRIBUTING.md if it exists).
    - Common dev tasks (running tests, linting, regenerating fixtures).
    - How to add a new component / feature, if there's an established
      pattern worth surfacing.
  Don't copy the whole README in — link to it and add what's missing.
-->
