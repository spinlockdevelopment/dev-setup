# Glossary

<!--
  AGENT: include only when the codebase uses domain-specific terms a
  newcomer would not know (acronyms, internal names, product-specific
  jargon). One term per entry, one or two sentences each. Skip generic
  software terms (REST, HTTP, JSON) — they don't belong in a project
  glossary.
-->
