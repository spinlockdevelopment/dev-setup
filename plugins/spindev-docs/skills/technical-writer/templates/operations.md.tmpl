# Operations

<!--
  AGENT: include this page only when the scan found ops surfaces
  (runbooks, alerts, SLOs, on-call notes). Cover:
    - How the team knows the system is healthy (alerts, dashboards, SLOs).
    - What "broken" looks like and the first 3 things to check.
    - Where logs and metrics live.
    - On-call rotation / escalation, if documented.
  Cross-link to runbook files in the repo rather than restating them.
-->
