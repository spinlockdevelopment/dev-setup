# Deployment

<!--
  AGENT: include this page only when the scan found deploy artifacts
  (Dockerfile, fly.toml, k8s manifests, deploy GH workflow). Cover:
    - Where the system runs in production (and any non-prod environments).
    - How a release is built and shipped (CI workflow → registry → host).
    - Secrets / config story (env vars, secret manager, mounted files).
    - Runtime topology (instance count, load balancer, CDN).
    - How to deploy manually if CI is broken.
  Reference real files (Dockerfile, fly.toml, .github/workflows/...) by
  path.
-->
