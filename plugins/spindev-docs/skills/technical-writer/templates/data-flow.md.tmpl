# Data flow

<!--
  AGENT: include this page only when the scan found queues, databases,
  message brokers, or cross-service calls. Cover:
    - The shape of data at rest (DB tables / collections / buckets) and
      who owns each.
    - The shape of data in motion (queues, topics, sync calls) and the
      direction of each edge.
    - Persistence vs caches.
    - Any back-pressure / retry / DLQ behavior worth knowing.
  A short sequence/flow diagram beats a long paragraph here.
-->
