# Architecture

<!--
  AGENT: write a high-level walkthrough of how the system is laid out.
  Cover:
    - Top-level modules / packages / services and what each is for.
    - The boundaries between them (which call which, in which direction).
    - Key cross-cutting concerns (auth, config, logging, telemetry).
    - Any in-process vs out-of-process splits.
  Prefer a short narrative + a labeled diagram or component list. If you
  can't justify a diagram (single-package repo), write a plain bullet list
  of top-level directories with a one-line purpose each.
-->
